<label class="control-label" for="frm_edcStartDate"><%= labelString %></label>
<div class="controls">
<% var d = new Date(); var month = 1 + d.getMonth();var day=d.getDate();
 var today = d.getFullYear() + '-' + (month < 10 ? '0' : '') + month + '-' + (day < 10 ? '0' : '') + day;
 d.setMonth(d.getMonth() -1); month = 1+ d.getMonth(); day = d.getDate();
 var lastMonth = d.getFullYear() + '-' + (month < 10 ? '0' : '') + month + '-' + (day < 10 ? '0' : '') + day;
 %>

<input name="from_date" class="input-date" value="<%= lastMonth %>" data-date-format="yyyy-mm-dd" language="zh-CN" type="text"
 style="width: 100px;" /> 到 <input name="to_date" class="input-date" type="text"  value="<%= today %>"
  language="zh-CN" data-date-format="yyyy-mm-dd" style="width: 100px;" />
</div>
