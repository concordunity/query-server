<table class="table table-condensed">
    <thead>
        <tr>
            <th><%= $.i18n._('label.user.username') %></th>
            <th><%= $.i18n._('label.user.fullname') %></th>
            <th><%= $.i18n._('label.user.role') %></th>
            <th>关区</th>
            <th>进出口类别</th>
            <th><%= $.i18n._('label.user.action') %></th>
        </tr>
    </thead>
    <tbody>
    
    <% for(var i = 0; i < this.length; i++) { %>
        <%== $.View('//docview/manage/accounts/users/views/user_row.ejs', this[i]) %>
    <% } %>
        
        
    </tbody>
</table>